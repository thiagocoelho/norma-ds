import { Meta, Title, IconGallery, IconItem } from '@storybook/blocks';
import { Box, Card,  Grid, Typography, Paper } from '@mui/material';
import { Icons as DefaultIconSVG} from "../components/Data/Icon/DefaultIcon"
import { Icons as NormaIconSVG} from "../components/Data/Icon/NormaIcon"
import { defaultIcons } from '../components/Data/Icon/default';
import { normaIcons } from '../components/Data/Icon/norma';

<Meta title="Documentation/Icons" />

# Icons

  <Grid container spacing={2}>
  <Grid item xs={12}>
      <Typography variant="h5">Norma DS</Typography>
    </Grid>
    {Object.keys(normaIcons).map((key) => (
      <Grid item xs={3} key={key}>
        <Paper sx={{padding: 2}}>
          <Typography variant={"body1"}> {key} </Typography>
          <NormaIconSVG icon={key} scale="xsmall" />
          <NormaIconSVG icon={key} scale="small"  color="secondary"/>
          <NormaIconSVG icon={key} scale="medium" color="error" />
          <NormaIconSVG icon={key} scale="large"  color="warning"/>
          <NormaIconSVG icon={key} scale="xlarge"  color="success" />
          <NormaIconSVG icon={key} scale="2xlarge"  color="black"/>
          <NormaIconSVG icon={key} scale="3xlarge" color="primary" />
        </Paper>
      </Grid>
      ))}
    <Grid item xs={12}>
      <Typography variant="h5">Default</Typography>
    </Grid>
    {Object.keys(defaultIcons).map((key) => (
      <Grid item xs={3} key={key}>
        <Paper sx={{padding: 2}}>
          <Typography variant={"body1"}> {key} </Typography>
          <DefaultIconSVG icon={key} scale="xsmall" />
          <DefaultIconSVG icon={key} scale="small"  color="secondary"/>
          <DefaultIconSVG icon={key} scale="medium" color="error" />
          <DefaultIconSVG icon={key} scale="large"  color="warning"/>
          <DefaultIconSVG icon={key} scale="xlarge"  color="success" />
          <DefaultIconSVG icon={key} scale="2xlarge"  color="black"/>
          <DefaultIconSVG icon={key} scale="3xlarge" color="primary" />
        </Paper>
      </Grid>
      ))}
  </Grid>
